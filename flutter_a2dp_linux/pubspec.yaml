name: flutter_a2dp_linux
description: A new flutter plugin project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_a2dp_platform_interface:
    path: ../flutter_a2dp_platform_interface

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  plugin:
    implements: flutter_a2dp
    platforms:
      linux:
        dartPluginClass: FlutterA2dpLinux
        pluginClass: none
